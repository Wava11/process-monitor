use super::Stage;

pub struct Process<'a> {
    name: String,
    first_stage: Stage<'a>,
}

// impl Process<'a> {
//     // pub fn new(name: String, stages: Vec<Binding>) -> Self {

//     // }
// }
